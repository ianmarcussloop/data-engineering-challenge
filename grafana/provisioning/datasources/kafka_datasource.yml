apiVersion: 1

datasources:
  - name: Kafka
    type: hamedkarbasi93-kafka-datasource
    access: proxy
    jsonData:
      bootstrapServers: "kafka:29092"  # ← Moved to jsonData, renamed
      clientId: grafana
      tlsAuth: false
      saslAuth: false
      schemaRegistryUrl: "http://schema-registry:8081"  # ← Required for Avro
    editable: true